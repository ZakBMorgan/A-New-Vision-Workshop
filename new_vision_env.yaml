# new_vision_env.yaml
# Command to install: conda env create -f new_vision_env.yaml
# Command to update: conda env update -f new_vision_env.yaml
name: new_vision_env
dependencies:
  - python=3.7.7
  - pip
  - matplotlib
  - numpy
  - opencv
  - scipy
  - wxpython
